class Solution:
    def findMaxLength(sef, nums):
        dict = {}
        for i in range(len(nums)):
            dict[i] = [0,0]
        for i in range(len(nums)):
            if nums[i] == 0:
                
        print(dict)
solution = Solution()
solution.findMaxLength([0,1,0,1,1])